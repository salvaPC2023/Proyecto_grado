# Copia este archivo a ".env" (ya esta en .gitignore) y completa tus valores reales.
# No subas ".env" al repo — solo ".env.example" se comitea.

DATABASE_URL=postgresql+psycopg2://usuario:password@localhost:5432/nombre_de_tu_bd

JWT_SECRET_KEY=cambia-esto-por-una-clave-larga-y-aleatoria
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=60
